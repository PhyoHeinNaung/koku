<x-guest-layout><p class="koku-eyebrow text-[var(--koku-indigo)]">One final step</p><h1 class="mt-3 font-serif text-3xl">Verify your email</h1><p class="mt-4 text-sm leading-6 text-[var(--koku-muted)]">We sent a verification link to your email address. Open it to complete your Koku account.</p>@if(session('status')==='verification-link-sent')<p class="mt-5 border border-[var(--koku-indigo)] p-3 text-xs text-[var(--koku-indigo)]">A new verification link has been sent.</p>@endif<div class="mt-8 flex items-center justify-between gap-4"><form method="POST" action="{{ route('verification.send') }}">@csrf<button class="bg-[var(--koku-indigo)] px-4 py-3 text-[10px] uppercase tracking-[0.12em] text-white">Resend email</button></form><form method="POST" action="{{ route('logout') }}">@csrf<button class="text-xs underline underline-offset-4">Sign out</button></form></div></x-guest-layout>

<x-guest-layout><p class="koku-eyebrow text-[var(--koku-indigo)]">Account recovery</p><h1 class="mt-3 font-serif text-3xl tracking-[-0.04em]">Reset your password</h1><p class="mt-4 text-sm leading-6 text-[var(--koku-muted)]">Enter your email and we will send you a secure reset link.</p><x-auth-session-status class="my-5" :status="session('status')"/><form method="POST" action="{{ route('password.email') }}" class="mt-7 space-y-5">@csrf<div><label class="koku-field-label" for="email">Email</label><input id="email" class="koku-field" type="email" name="email" value="{{ old('email') }}" required autofocus>@error('email')<p class="koku-field-error">{{ $message }}</p>@enderror</div><button class="w-full bg-[var(--koku-indigo)] px-5 py-4 text-xs uppercase tracking-[0.14em] text-white">Send reset link</button><a href="{{ route('login') }}" class="block text-center text-xs underline underline-offset-4">Back to sign in</a></form></x-guest-layout>

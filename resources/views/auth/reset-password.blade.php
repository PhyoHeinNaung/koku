<x-guest-layout><p class="koku-eyebrow text-[var(--koku-indigo)]">Account recovery</p><h1 class="mt-3 font-serif text-3xl">Choose a new password</h1><form method="POST" action="{{ route('password.store') }}" class="mt-8 space-y-5">@csrf<input type="hidden" name="token" value="{{ $request->route('token') }}"><div><label class="koku-field-label">Email</label><input class="koku-field" type="email" name="email" value="{{ old('email',$request->email) }}" required autofocus>@error('email')<p class="koku-field-error">{{ $message }}</p>@enderror</div><div><label class="koku-field-label">New password</label><input class="koku-field" type="password" name="password" required autocomplete="new-password">@error('password')<p class="koku-field-error">{{ $message }}</p>@enderror</div><div><label class="koku-field-label">Confirm password</label><input class="koku-field" type="password" name="password_confirmation" required autocomplete="new-password"></div><button class="w-full bg-[var(--koku-indigo)] px-5 py-4 text-xs uppercase tracking-[0.14em] text-white">Reset password</button></form></x-guest-layout>
